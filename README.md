# Simple Media Network

Next.js launch repo for the Simple Media Network site.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run start`

## Notes

- The homepage is rendered from `simple-media-network-source/index.html` so the existing design remains the canonical source during launch.
- Shared assets are served from `public/`.
